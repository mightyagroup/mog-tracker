Drop IronHouse's primary logo here as logo.png when ready.

The document generator uses a text wordmark fallback until the file exists,
so you can leave this folder empty and the proposal platform keeps working.

Recommended:
- PNG with transparent background
- 1000px wide or larger
- 300+ DPI
